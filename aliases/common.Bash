# Common Utility Aliases
alias vi='vim'
alias wget='wget -c'

# Make basic commands interactive, and verbose
alias cp='cp -iv'
alias rm='rm -i'
alias mv='mv -iv'
alias grep='grep -i'
alias mkdir='mkdir -pv'
